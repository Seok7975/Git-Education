1. C:\git-test4 에 로컬 저장소를 만든다. 그리고 A가 적힌 a.txt 파일을 만들고 커밋한다. 커밋 메시지는 1로 하자.
2. 이번엔 b.txt 파일을 만들고 B를 적은 후 커밋한다. 커밋 메시지는 2로 하자.
3. 마지막으로 c.txt 파일을 만들고 C를 적은 후 커밋한다. 커밋 메시지는 3으로 한다.
4. 이렇게 세 개의 커밋을 만들었다. 여기서 foo라는 브랜치를 만들고, foo 브랜치로 체크아웃하자.
5. 이제 foo_d.txt 파일을 만들고 D를 적은 후 커밋한다. 커밋 메시지는 4로 하자.
6. foo_e.txt 파일을 만들고 E를 적은 후 커밋한다. 커밋 메시지는 5로 하자.
7. master 브랜치로 체크아웃해 보자.
8. bar라는 새로운 브랜치를 만들어 보자.
9. bar_d.txt 파일을 만들고 D를 적은 후 커밋한다. 커밋 메시지는 4로 한다. 
10. bar_e.txt 파일을 만들고 E를 적은 후 커밋한다. 커밋 메시지는 5로 한다.
11. bar_f.txt 파일을 만들고 F를 적은 후 커밋한다. 커밋 메시지는 6으로 한다.

12. foo 브랜치를 master 브랜치로 병합해 보자.
13. foo 브랜치를 삭제하자.
14. 상단 병합을 이용하여 bar 브랜치의 4번 커밋을 master 브랜치로 병합하자.
15.  bar 브랜치의 5번 커밋과 6번 커밋을 master 브랜치로 한 번에 병합해 보자. 

1. c:\git-test5 로컬 저장소를 만들고 master 브랜치에 A가 저장된 a.txt 파일을 만든 뒤 이를 커밋합니다. 커밋 메시지는 first로 하겠습니다.
2. foo 브랜치를 생성하고, 생성한 foo 브랜치로 체크아웃합니다. 
3. a.txt 파일에 적힌 A를 foo로 변경한 뒤 커밋합니다. 커밋 메시지는 foo로 하겠습니다.
4. master 브랜치로 체크아웃합니다. a.txt 파일에 적힌 A를 master로 변경한 뒤 커밋합니다. 커밋 메시지는 master로 하겠습니다.
5. master 브랜치로 foo 브랜치를 병합하자. 충돌 해결은 '내것'을 이용해 해결하자.
6. 기본 메시지로 커밋한다.

1. c:\git-test6에 로컬 저장소를 만든다. 
2. A가 저장된 a.txt 파일을 만들고 커밋한다. 커밋 메시지는 1로 한다.
3. B가 저장된 b.txt 파일을 만들고 커밋한다. 커밋 메시지는 2로 한다.
4. 여기서 foo라는 새로운 브랜치를 만든 뒤 체크아웃합니다.
5. C가 저장된 foo_c.txt 파일을 만들고 커밋한다. 커밋 메시지는 3으로 한다.
6. D가 저장된 foo_d.txt 파일을 만들고 커밋한다. 커밋 메시지는 4로 한다.
7. master 브랜치로 체크아웃합니다.
8. C가 저장된 c.txt 파일을 만들고 커밋한다. 커밋 메시지는 3으로 한다.
9. D가 저장된 d.txt 파일을 만들고 커밋한다. 커밋 메시지는 4로 한다.
10. foo 브랜치로 체크아웃합니다.
11. master의 네 번째 커밋으로 재배치합니다.



