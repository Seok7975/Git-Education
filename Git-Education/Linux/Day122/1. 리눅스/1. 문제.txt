해당하는 명령어를 쓰시오.

		슈퍼 사용자로 전환하기
		전원 끄기
		재부팅
 		현재 시간을 출력하거나 설정
 		인자로 인정한 문자열을 출력
		로그인 쉘 확인하기
		현재 사용중인 셸을 sh로 변경
		sh로 바꾼 상태에서 bash  실행
		한 문자 뒤로 이동(backward)
		한 문자 앞으로 이동(forward)
		커서를 맨 앞으로 이동
		커서를 맨 뒤로 이동
		한 단어 뒤로 이동
		한 단어 앞으로 이동
		커서 위치 기준으로 뒤에 있는 한 문자를 삭제
		커서 위치의 한 문자를 삭제
		커서 뒤에 공백이 나오기 전까지 있는 문자들(단어)를 삭제
		커서의 위치에서 끝까지 삭제
		커서의 위치에서 커맨드 라인의 첫 문자까지 삭제
		마지막으로 지운 내용을 삽입
		화면 표시를 잠근다.
		화면 표시 잠금을 해제한다.
		화면을 삭제한다.
 		바로 전 명령으로 이동한다. (previous)
		다음 명령으로 이동한다.(next)
		명령 이력을 검색

중분검색
(문자 입력)	문자를 하나씩 입력할 때마다 검색을 수행합니다.
		한 개 이전의 검색 결과로 이동합니다.
		현재 검색 결과를 실행합니다.
		현재 검색 결과를 실행하지 않은 채 커맨드 라인으로 복귀합니다.
		검색 결과를 지우고 프롬프트로 복귀합니다.
		현재 어떤 문자 코드가 사용되는지 확인
		현재 디렉터리 출력
		/usr 디렉터리로 이동
		홈 디렉터리로 이동
		루트 디렉터리의 목록을 출력
		/bin/cp의 목록을 출력
		/ 와 /usr의 목록을 출력

경로 확장에서 사용하는 기호
		임의의 문자열
		임의의 한 문자

		ba로 시작하는 목록을 출력하는 코드
		확장자가 html로 끝나는 목록을 출력하는 코드
		ba로 시작하고 뒤에 두자가 있는 목록을 출력하는 코드

		-l 옵션으로 상세 정보를 함께 출력
		-a 옵션으로 숨겨진 파일도 출력

-F 옵션으로 표시되는 파일의 종류

종류		기호
	표시되지 않음
	/
	*
	@	

	리스트를 보는데 문자를 30개씩 출력. 